% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey_template.R
\name{lst_settings}
\alias{lst_settings}
\title{List survey settings templates}
\usage{
lst_settings(example = "minimal", lang = "en", add_valid = FALSE, path = NULL)
}
\arguments{
\item{example}{Character. One of \code{"minimal"}, \code{"simple"}, or \code{"full"}.}

\item{lang}{Character vector containing \code{"en"}, \code{"de"}, or both.}

\item{add_valid}{Logical. If \code{TRUE}, printed or written YAML adds inline
comments with valid values or expected input types.}

\item{path}{Optional output path. May point to a file
or to an existing directory.}
}
\value{
A settings list. When \code{path} is \code{NULL}, the object is returned
visibly and prints as YAML when not assigned. When \code{path} is supplied, YAML
is written and the object is returned invisibly.
}
\description{
Creates starter templates for the \code{settings} part of a LimeSeed seed.
}
\examples{
lst_settings()
lst_settings(add_valid = TRUE)
x <- lst_settings("simple", lang = c("en", "de"))
lst_settings("full", path = tempdir())
}
