% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey_help.R
\name{ls_questions}
\alias{ls_questions}
\title{List available LimeSurvey question types}
\usage{
ls_questions(type = "all", detail = FALSE, search = NULL)
}
\arguments{
\item{type}{Character. \code{"all"} (default), a single type code or label, or a
character vector of codes/labels.}

\item{detail}{Logical. If \code{TRUE}, also print applicable options per type.}

\item{search}{Character. Optional case-insensitive substring filter applied
to type codes, labels, and descriptions.}
}
\value{
Invisibly, a \code{data.frame} with columns \code{code}, \code{label}, \code{labels},
\code{requires}, \code{quota}, \code{n_options}.
}
\description{
Prints an overview of all (or selected) question types: their LS type code,
primary label, additional label synonyms, structural requirements, and
whether the type supports quota membership.
}
\details{
When \code{detail = TRUE}, each type entry is followed by a table of all
applicable attribute options with defaults and valid values.
}
\examples{
ls_questions()
ls_questions("L")
ls_questions(c("L", "M", "F"), detail = TRUE)
ls_questions(search = "array")
}
\seealso{
\code{\link[=ls_options]{ls_options()}}, \code{\link[=ls_question_build]{ls_question_build()}}
}
