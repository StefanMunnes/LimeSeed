% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey_help.R
\name{ls_question_build}
\alias{ls_question_build}
\title{Generate a copy-paste YAML scaffold for one or more question types}
\usage{
ls_question_build(
  type = "all",
  options = "required",
  langs = NULL,
  code = "QCODE"
)
}
\arguments{
\item{type}{Character. A type code or label, a character vector, or \code{"all"}
to generate scaffolds for every registered type.}

\item{options}{Character. Controls the breadth of included fields:
\describe{
\item{\code{"required"}}{\code{type} + \code{questionTexts} + structural fields
(\code{answerOptions} / \code{subquestions}) as required by the type.}
\item{\code{"defaults"}}{Everything in \code{"required"} plus the most commonly
set core fields with their default values (\code{mandatory}, \code{other},
\code{relevance}).}
\item{\code{"all"}}{Everything in \code{"defaults"} plus every applicable
type-specific attribute option, commented out with its default value
and valid values for easy discovery.}
}}

\item{langs}{Character vector of language codes for multilingual output
(e.g. \code{c("en", "de")}), or \code{NULL} for single-language placeholders.}

\item{code}{Character. Placeholder question code in the scaffold
(default \code{"QCODE"}).  When multiple types are requested, the type code is
appended automatically (e.g. \code{QCODE_L}, \code{QCODE_M}).}
}
\value{
Invisibly, a character vector of the printed YAML lines.
}
\description{
Prints a ready-to-use YAML skeleton that can be pasted directly into the
\verb{structure:} \u203a \verb{<GroupCode>:} block of a LimeSeed YAML file.  Each
scaffold is preceded by a short comment describing the type and its requirements.
}
\examples{
# Single type — minimal scaffold
ls_question_build("L")

# Single type — all options shown (commented)
ls_question_build("L", options = "all")

# Multilingual scaffold
ls_question_build("L", langs = c("en", "de"))

# Multiple types at once
ls_question_build(c("S", "L", "M", "F"))

# Capture output to a file
lines <- capture.output(ls_question_build("F", options = "defaults", langs = c("en", "de")))
writeLines(lines, "question_scaffold.yaml")
}
\seealso{
\code{\link[=ls_questions]{ls_questions()}}, \code{\link[=ls_options]{ls_options()}}, \code{\link[=ls_settings_build]{ls_settings_build()}}
}
