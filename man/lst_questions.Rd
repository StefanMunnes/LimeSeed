% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey_template.R
\name{lst_questions}
\alias{lst_questions}
\title{List question templates}
\usage{
lst_questions(
  types = "all",
  example = "minimal",
  lang = "en",
  add_valid = FALSE,
  path = NULL
)
}
\arguments{
\item{types}{Character vector of question types, search terms, or \code{"all"}.}

\item{example}{Character. One of \code{"minimal"}, \code{"simple"}, or \code{"full"}.}

\item{lang}{Character vector containing \code{"en"}, \code{"de"}, or both.}

\item{add_valid}{Logical. If \code{TRUE}, printed or written YAML adds inline
comments with valid values or expected input types.}

\item{path}{Optional output path. May point to a file
or to an existing directory.}
}
\value{
A named list of questions. When \code{path} is \code{NULL}, the object is
returned visibly and prints as YAML when not assigned. When \code{path} is
supplied, YAML is written and the object is returned invisibly.
}
\description{
Creates starter templates for survey questions without wrapping them in
groups. Use \code{\link[=lst_seed]{lst_seed()}} to build a complete survey seed with group
structure. \code{example = "full"} adds all applicable question option fields for
the selected type(s).
}
\examples{
lst_questions()
lst_questions(c("radio", "array"), example = "minimal")
lst_questions("radio", example = "simple")
lst_questions("radio", example = "full", add_valid = TRUE)
qs <- lst_questions("long text", example = "full", lang = "de")
lst_questions("radio", path = tempdir())
}
