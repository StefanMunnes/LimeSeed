% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey_template.R
\name{lst_seed}
\alias{lst_seed}
\title{List a full seed template}
\usage{
lst_seed(
  example = "minimal",
  question_types = c("radio", "long text"),
  quota = FALSE,
  lang = "en",
  add_valid = FALSE,
  path = NULL
)
}
\arguments{
\item{example}{Character. One of \code{"minimal"}, \code{"simple"}, or \code{"full"}. Controls both the
settings and question templates. \code{"full"} also adds \code{groupOptions} to the
sample group.}

\item{question_types}{Character vector of question types, search terms, or
\code{"all"}.}

\item{quota}{Logical. Whether to include a quota template.}

\item{lang}{Character vector containing \code{"en"}, \code{"de"}, or both.}

\item{add_valid}{Logical. If \code{TRUE}, printed or written YAML adds inline
comments with valid values or expected input types.}

\item{path}{Optional output path. An existing directory
writes split \code{settings.yml}, \code{structure.yml}, and \code{quota.yml}; otherwise a
single combined YAML file is written.}
}
\value{
A full seed list with \code{settings}, \code{structure}, and optional \code{quota},
returned visibly when \code{path} is \code{NULL} and invisibly otherwise.
}
\description{
Combines \code{lst_settings()}, \code{lst_questions()}, and \code{lst_quota()} into one
usable starter seed. \code{example = "full"} adds all applicable question option
fields for the selected type(s).
}
\examples{
lst_seed()
seed <- lst_seed(
  example = "full",
  question_types = c("radio", "array"),
  quota = TRUE
)
lst_seed(example = "full", add_valid = TRUE)
lst_seed(path = tempdir())
}
