% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey_validate.R
\name{validate_seed}
\alias{validate_seed}
\title{Validate a LimeSeed survey definition}
\usage{
validate_seed(seed, file = NULL, stop_on_error = TRUE)
}
\arguments{
\item{seed}{Survey definition in any format accepted by \code{\link[=load_seed]{load_seed()}}.}

\item{file}{Optional file path (\code{character(1)}).  When supplied, the full
validation report is written as plain text in addition to being printed
on the console.  The parent directory must already exist.}

\item{stop_on_error}{Logical (default \code{TRUE}).  Set to \code{FALSE} to demote
errors to warnings so the function always returns without calling
\code{\link[=stop]{stop()}} — useful in scripts that want to collect all issues
programmatically before deciding how to proceed.}
}
\value{
A data frame of issues (columns: \code{severity}, \code{location}, \code{message}),
returned invisibly.  Zero rows means validation passed.
}
\description{
Checks a seed for structural and content issues before building.  All issues
are collected in a single pass and reported at once — nothing is silently
swallowed or truncated.
}
\details{
Accepts the same input formats as \code{\link[=load_seed]{load_seed()}}: a file path, folder path,
named list of paths, or a pre-loaded seed list.
}
\section{Output behaviour}{

Full issue detail is always printed via \code{\link[=message]{message()}} — one line per issue —
so the console output is never truncated regardless of how many issues are
found.  \code{warning()} and \code{stop()} carry only a short count summary.

After printing:
\itemize{
\item \strong{Errors} call \code{\link[=stop]{stop()}} with a short count summary.
Build cannot proceed.
\item \strong{Warnings only} call \code{\link[=warning]{warning()}} with a short count summary.
The issues data frame is still returned.
\item \strong{No issues} emit a single pass message and return a zero-row
data frame.
}
}

\section{Return value}{

Always returns a \strong{data frame of issues} invisibly — including when
validation passes (zero-row data frame).  This lets you capture, filter,
and export results without re-running validation.

The data frame has three character columns:
\describe{
\item{\code{severity}}{\code{"error"} or \code{"warning"}.}
\item{\code{location}}{Dot-path to the offending element,
e.g. \code{"structure$G1$Q2$hidden"}.}
\item{\code{message}}{Human-readable description of the issue.}
}
}

\examples{
\dontrun{
# ── Standalone use ───────────────────────────────────────────────────────

# Validate and let errors stop execution
validate_seed("path/to/survey.yaml")

# Capture the issues data frame for programmatic inspection
issues <- validate_seed("path/to/survey.yaml")
issues[issues$severity == "error", ]

# Write a persistent plain-text report to disk
validate_seed("path/to/survey.yaml", file = "reports/validation.txt")

# Export issues as CSV
issues <- validate_seed("path/to/survey.yaml", stop_on_error = FALSE)
write.csv(issues, "reports/validation.csv", row.names = FALSE)

# Collect all issues without stopping — useful in CI / batch scripts
issues <- validate_seed("path/to/survey.yaml", stop_on_error = FALSE)
if (any(issues$severity == "error")) {
  # handle errors programmatically
}

# ── Pipeline use ─────────────────────────────────────────────────────────

# validate_seed() load seed and stops the pipe on errors, passes through on warnings

validate_seed("path/to/survey.yaml") |>
  build_lsdf() |>
  write_lsdf("output/survey.tsv")
}

}
\seealso{
\code{\link[=load_seed]{load_seed()}}, \code{\link[=build_lsdf]{build_lsdf()}}, \code{\link[=seed_to_tsv]{seed_to_tsv()}}
}
