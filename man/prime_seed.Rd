% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seed_to_tsv.R
\name{prime_seed}
\alias{prime_seed}
\title{Load and normalise a LimeSeed survey definition, so called seed}
\usage{
prime_seed(seed)
}
\arguments{
\item{seed}{Survey definition in one of the following formats:

\strong{1. Single YAML file path} (\code{character(1)}) \cr
Path to a \code{.yml} / \code{.yaml} file whose top-level keys include at least
\code{settings} and \code{structure}.  An optional \code{quota} key is also read if
present.

\strong{2. YAML folder path} (\code{character(1)}) \cr
Path to a directory containing separate YAML files named
\code{settings.yml}, \code{structure.yml}, and optionally \code{quota.yml}
(\code{.yaml} extensions are also accepted, case-insensitively).
Files with any other names in the same directory are silently ignored.

\strong{3. Named list of file paths} (\code{list}) \cr
A named list where every element is a length-1 character string
pointing to a YAML file.  Required names: \code{"settings"}, \code{"structure"}.
Optional name: \code{"quota"}.  Each file is read with \code{\link[yaml:read_yaml]{yaml::read_yaml()}}.

\strong{4. Mixed named list} (\code{list}) \cr
A named list whose elements are either a length-1 character path to a
YAML file \emph{or} an already-parsed R list.  Useful when one component
(e.g. \code{structure}) is built programmatically while the other
(e.g. \code{settings}) is still read from disk.  Required names and
optional names are the same as in case 3.

\strong{5. Pre-loaded sprout list} (\code{list}) \cr
A fully parsed named list that already contains \code{settings},
\code{structure}, and optionally \code{quota} as R lists.  Returned unchanged.
Passing a sprout through \code{prime_seed()} is therefore idempotent and
safe to use in pipelines where the upstream format is not known in
advance.}
}
\value{
A named list — the \emph{sprout} — with the following elements:
\describe{
\item{\code{settings}}{Named list of survey-level settings.}
\item{\code{structure}}{Named list of groups, questions, subquestions,
and answer options.}
\item{\code{quota}}{Named list of quota definitions, or \code{NULL} when no
quota is defined.}
}
}
\description{
Accepts a survey definition in any of five formats and returns a
consistently structured named list (a \emph{sprout}) ready for manipulation
or direct use by \code{\link[=build_lsdf]{build_lsdf()}}.
}
\section{Errors}{

\code{prime_seed()} calls \code{\link[=stop]{stop()}} in the following situations:
\itemize{
\item The path in \code{seed} does not exist.
\item A single-file path does not have a \code{.yml} or \code{.yaml} extension.
\item A single YAML file is missing the \code{settings} or \code{structure}
top-level keys.
\item A folder contains fewer than two recognised YAML files
(\code{settings}, \code{structure}).
\item A list \code{seed} is unnamed or has any unnamed elements.
\item A list \code{seed} is missing the \code{"settings"} or \code{"structure"} keys.
\item In case 4, a list element is neither a valid file path nor an
R list.
}
}

\examples{
\dontrun{
# Case 1 — single combined YAML file
sprout <- prime_seed("path/to/survey.yaml")

# Case 2 — folder with settings.yml, structure.yml, quota.yml
sprout <- prime_seed("path/to/survey_dir/")

# Case 3 — named list of file paths
sprout <- prime_seed(list(
  settings  = "path/to/settings.yaml",
  structure = "path/to/structure.yaml",
  quota     = "path/to/quota.yaml"
))

# Case 4 — mixed: one file path, one pre-built R list
my_structure <- list(G1 = list(Q1 = list(type = "S", questionTexts = "Name?")))
sprout <- prime_seed(list(
  settings  = "path/to/settings.yaml",
  structure = my_structure
))

# Case 5 — already a sprout; returned unchanged (idempotent)
sprout <- prime_seed(sprout)

# Typical manipulation workflow
sprout <- prime_seed("path/to/survey.yaml")
sprout$settings$anonymized          <- "N"
sprout$structure$G1$Q2$relevance    <- "Q1 == 'yes'"
df <- build_lsdf(sprout)
}

}
\seealso{
\code{\link[=build_lsdf]{build_lsdf()}} to convert the sprout into a LimeSurvey data
frame; \code{\link[=seed_to_tsv]{seed_to_tsv()}} for the all-in-one shortcut.
}
